# IEEE Quantum Week 2026: Probabilistic error cancellation with logical noise models

To run this notebook guide, first ``pip install -r requirements.txt``. Next, execute the notebook with the .npz data saved to disk. Play with the QPU data and build up a familiarity with the interfaces and data structures being used in the guide.
